{
    "buildCommand": "cd example && bun install && bun run predeploy",
    "outputDirectory": "example/dist",
    "devCommand": "expo",
    "cleanUrls": true,
    "framework": null,
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "/"
      }
  ]
}